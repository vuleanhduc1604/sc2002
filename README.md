# SC2002 Turn-Based Combat Game

## How to Run

From the project root directory, compile and run with:

```bash
find src/main/java -name "*.java" | xargs javac -d out && java -cp out com.game.controller.GameController
```
